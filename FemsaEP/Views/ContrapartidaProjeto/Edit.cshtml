@model FemsaEP.Models.contrapartida_projeto

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>contrapartida_projeto</h4>
        <hr />
        @Html.ValidationSummary(true)
        <div class="form-group">
            @Html.LabelFor(model => model.id_projeto, "id_projeto", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("id_projeto", String.Empty)
                @Html.ValidationMessageFor(model => model.id_projeto)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.id_contrapartida, "id_contrapartida", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("id_contrapartida", String.Empty)
                @Html.ValidationMessageFor(model => model.id_contrapartida)
            </div>
        </div>

        @Html.HiddenFor(model => model.id)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
